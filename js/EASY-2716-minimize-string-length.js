/**
 * @param {string} s
 * @return {number}
 */
var minimizedStringLength = function (s) {
  return s
}

const tests = ['aaabc']
// const tests = ['aaabc', 'cbbd', 'dddaaa']

for (const test of tests) {
  console.log(minimizedStringLength(test))
}
